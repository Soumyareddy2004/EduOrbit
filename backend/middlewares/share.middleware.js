// exports.validatePost = (req, res, next) => {
//     const { profileLinks, achievements, roadmap, suggestions } = req.body;
//     if (!profileLinks || !achievements || !roadmap || !suggestions) {
//       return res.status(400).json({ error: "All fields are required." });
//     }
//     next();
//   };
  
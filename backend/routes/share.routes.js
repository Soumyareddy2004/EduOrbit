// const express = require("express");
// const router = express.Router();
// const shareController = require("../controllers/share.controller");

// // Routes for Share API
// router.post("/", shareController.createPost); // Create a post
// router.get("/", shareController.getAllPosts); // Get all posts
// router.get("/:id", shareController.getPostById); // Get a post by ID
// router.put("/:id", shareController.updatePost); // Update a post by ID
// router.delete("/:id", shareController.deletePost); // Delete a post by ID

// module.exports = router;
